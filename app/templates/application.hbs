<h1>Community Board Budget Requests App</h1>

{{stateful-component thing=thing}}

{{!-- The following component displays Ember's default welcome message. --}}
{{!-- {{welcome-page}} --}}
{{!-- Feel free to remove this! --}}

<div class="layout-row flex">
  <div class="flex">
    Listing of budget requests 
    <ul>
      {{#each model as |budgetRequest|}}
        <li>
          {{link-to budgetRequest.need 'request' budgetRequest.id}} 
        </li>
      {{/each}}
    </ul>
  </div>
  <div class="flex">
    Current View:
    {{outlet}}
  </div>
</div>

{{!-- {{component-name property1='something' f}} --}}
