import numberRounder from 'budget-request-app/utils/number-rounder';
import { module, test } from 'qunit';

module('Unit | Utility | number rounder');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numberRounder();
  assert.ok(result);
});
